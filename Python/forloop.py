# Note: Python starts counting at 0, not 1.
# A for loop that executes 10 times will start at 0 and end at 9.

for i in range(0, 10):
    print("This value will increment each time this loop runs until it reaches 9! Value is " + str(i))